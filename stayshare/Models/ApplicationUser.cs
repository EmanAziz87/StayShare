using Microsoft.AspNetCore.Identity;

namespace stayshare.Models;

public class ApplicationUser : IdentityUser
{
    // Add any additional properties from your current User model
    // that aren't already in IdentityUser
}
